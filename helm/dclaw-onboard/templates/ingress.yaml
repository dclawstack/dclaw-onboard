apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "dclaw-onboard.name" . }}
  labels:
    app.kubernetes.io/name: {{ include "dclaw-onboard.name" . }}
spec:
  rules:
    - host: dclaw-onboard.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ include "dclaw-onboard.name" . }}
                port:
                  number: {{ .Values.frontend.service.port }}
